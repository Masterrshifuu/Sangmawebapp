
'use client';

// This file is no longer used for the main homepage, 
// as that logic has been moved to src/app/page.tsx.
// It is kept to prevent breaking imports in the AppShell, but it renders nothing.

export default function HomePageContent() {
  return null;
}
